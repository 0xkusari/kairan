<%= form_for channel do |f| %>
  <div class="field">
    <%= f.label :url %>
    <%= f.text_field :url, placeholder: "https://~~~" %>
  </div>
  <div class="actions">
    <%= f.submit "Add" %>
  </div>
<% end %>
